[build]
  command = "npm run build"
  publish = "dist"

# React/Vite SPA: todas as rotas servem index.html (status 200, sem redirect no browser)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
